#List of testing data sets:

#1. HANDWRITTEN DIGITS
##digits = load_digits()
##n_samples = len(digits.images)
##data = digits.images.reshape((n_samples, -1))
##X = data[:n_samples // 2]
##y = digits.target[:n_samples // 2]
##testX = data[n_samples // 2:]
##testY = digits.target[n_samples // 2:]
